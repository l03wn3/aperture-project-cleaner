to replaceText(someText, oldItem, newItem)	(*     replace all occurances of oldItem with newItem          parameters -     someText [text]: the text containing the item(s) to change                    oldItem [text, list of text]: the item to be replaced                    newItem [text]: the item to replace with          returns [text]:     the text with the item(s) replaced     *)	set {tempTID, AppleScript's text item delimiters} to {AppleScript's text item delimiters, oldItem}	try		set {itemList, AppleScript's text item delimiters} to {text items of someText, newItem}		set {someText, AppleScript's text item delimiters} to {itemList as text, tempTID}	on error errorMessage number errorNumber -- oops		set AppleScript's text item delimiters to tempTID		error errorMessage number errorNumber -- pass it on	end try		return someTextend replaceTexttell application "Aperture"	activate	set imageSel to (get selection)	set proj to (get project)	if imageSel is {} then		error "Please select an image."	else		set selectedImage to item 1 of imageSel		set proj to parent of selectedImage		set newAlbumName to "duplicates"		tell proj			if album newAlbumName of proj exists then							else				make new album with properties {name:newAlbumName}			end if						set dupAlbum to album newAlbumName of proj		end tell		set images to image versions of proj				repeat with i from 1 to count of images			set image to item i of images			set fileName to (get the value of the other tag "FileName" of image)						if (fileName contains ".JPG") or (fileName contains ".jpg") then				set newName to my replaceText(fileName, ".JPG", ".RAF")								repeat with i from 1 to count of images					set newImage to item i of images					set newFileName to (get the value of the other tag "FileName" of newImage)					if newName is newFileName then						set rating to main rating of newImage						#if raw file rating is 4 or above, we keep the raw, but delete the jpg						if rating > 3 then							tell dupAlbum								duplicate image to dupAlbum							end tell						end if						# if raw file rating is -1, we delete both the raw and the jpg						if rating < 0 then							tell dupAlbum								duplicate image to dupAlbum								duplicate newImage to dupAlbum							end tell													end if											end if				end repeat			else				# raw file				set rating to main rating of image				if rating < 4 then					tell dupAlbum						duplicate image to dupAlbum					end tell				end if			end if		end repeat	end if	display dialog "Done!"end tell